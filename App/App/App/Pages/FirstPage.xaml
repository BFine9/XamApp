<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App.Pages.FirstPage"
             Title="Aplikacja">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="XamarinLogo" Aspect="AspectFit" VerticalOptions="EndAndExpand"></Image>
        <Button Grid.Row="1" Text="Kliknij by zobaczyć użytkowników" Command="{Binding ShowUsers}" FontSize="Medium" VerticalOptions="End" Margin="0, 20"></Button>
    </Grid>
</ContentPage>
